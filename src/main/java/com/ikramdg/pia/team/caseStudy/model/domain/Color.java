package com.ikramdg.pia.team.caseStudy.model.domain;

import javax.persistence.Embeddable;

@Embeddable
public enum Color {
	RED, YELLOW, BLUE, WHITE, BLACK, GREEN, VIOLET, ORANGE; // each is an instance of Color

}
